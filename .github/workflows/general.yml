name: Deployment of fe cms gwt

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    # Definisikan array server di matrix
    strategy:
      matrix:
        server:
          - name: VPS-A         # matrix.server.name pertama
            host: 147.93.30.80  # matrix.server.host pertama
            secret: VPS1_PASSWORD
          - name: VPS-B
            host: 165.232.165.118
            secret: VPS2_PASSWORD

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Load testing env file
        run: |
          if [ -f .env.testing ]; then
            cp .env.testing .env
          fi

      - name: Inject API base URL from secret
        run: |
          echo "NEXT_PUBLIC_API_BASE_URL=${{ secrets.API_BASE_URL }}" >> .env

      - name: Install dependencies & build
        run: |
          npm install --legacy-peer-deps
          npm run build

      - name: Deploy to ${{ matrix.server.name }}
        if: github.ref == 'refs/heads/main'
        run: |
          sshpass -p "${{ secrets[ matrix.server.secret ] }}" rsync -avz --delete --exclude='.env*' \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./ root@${{ matrix.server.host }}:/var/www/fe-cms-gwt/

      - name: Restart on ${{ matrix.server.name }}
        run: |
          sshpass -p "${{ secrets[ matrix.server.secret ] }}" ssh -o StrictHostKeyChecking=no root@${{ matrix.server.host }} << 'EOF'
            systemctl restart nginx
            pm2 restart fe-cms-gwt
          EOF
